services:
  pagecraft:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./prompts:/app/prompts
    environment:
      - PAGECRAFT_DB_PATH=/app/data/pagecraft.db
      - PAGECRAFT_DEBUG=true
